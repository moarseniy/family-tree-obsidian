{
  "name": "family-tree-plugin",
  "version": "0.0.1",
  "description": "A plugin that automatically generates an interactive family tree from your Obsidian notes. Simply create notes with parent links in YAML frontmatter, and the plugin visualizes relationships as an inverted tree (you at the base).",
  "main": "lib/main.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --external:obsidian --target=es2019 --outfile=main.js"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "typescript": "^4.9.0",
    "obsidian": "obsidianmd/obsidian-api",
    "obsidian-plugin-cli": "^0.4.5"
  },
  "dependencies": {
    "mermaid": "^10.0.0",
    "panzoom": "^9.4.3"
  }
}
